{
  "name": "@aztlan/grid",
  "version": "0.0.1-alpha.0",
  "license": "MPLv2",
  "scripts": {
    "compile": "sass src/main.scss dist/main.css",
    "filesize": "du --apparent-size -sh ${1}",
    "lint:stylelint": "stylelint src/**/*.scss --rd --rdd --risd --color --formatter verbose",
    "lint:stylelint:fix": "npm run lint:stylelint -- --fix"
  },
  "devDependencies": {
    "@aztlan/stylelint-config": "^0.0.1-alpha.0",
    "postcss-scss": "^4.0.9",
    "sass": "^1.69.4"
  }
}
