{
  "name": "@aztlan/bem",
  "version": "0.7.0",
  "license": "MPLv2",
  "style": "index.scss",
  "main": "index.js",
  "types": "index.ts",
  "scripts": {
    "lint:stylelint": "stylelint **/*.scss --rd --rdd --risd --color --formatter verbose",
    "compile:ts": "./scss-to-ts.js index.scss index.js",
    "compile:types": "tsc index.js --declaration --emitDeclarationOnly --target ES2022 --outDir . --allowJs",
    "prepublishOnly": "npm run compile:ts && npm run compile:types"
  },
  "devDependencies": {
    "@aztlan/stylelint-config": "^0.7.0",
    "typescript": "^5.3.3"
  },
  "dependencies": {
    "get-sass-vars": "^4.0.2"
  }
}
